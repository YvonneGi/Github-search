import { BolderDirective } from './bolder.directive';

describe('BolderDirective', () => {
  it('should create an instance', () => {
    const directive = new BolderDirective();
    expect(directive).toBeTruthy();
  });
});
